// next.config.js
module.exports = {
    reactStrictMode: true, // Enables React Strict Mode (helps with identifying potential problems in the app)
    images: {
      domains: ['example.com'], // Allows images to be loaded from 'example.com'
    },
  };
  images: {
    domains: ['myimages.com', 'cdn.example.com']}
    
    